echo "$PWD"

echo "[*] Testing"
#npm run test

./scripts/run-tests.sh

echo "$PWD"

RESULT=$?
[ $RESULT -ne 0 ] && exit 1


echo "[*] OAS Lint"

npm run spectral:oas:lint:one

RESULT=$?
[ $RESULT -ne 0 ] && exit 1
exit 0